<div class="max-w-4xl mx-auto p-6">
  <h1 class="text-2xl font-bold mb-6">자녀 학습 현황</h1>

  <% if @children.any? %>
    <div class="space-y-4">
      <% @children.each do |child| %>
        <div class="border rounded-lg p-4">
          <div class="flex justify-between items-center">
            <div>
              <h2 class="font-semibold text-lg"><%= child.name %></h2>
              <p class="text-sm text-gray-500"><%= child.grade_level %>학년 | 완료 학습: <%= child.learning_sessions.completed.count %>회</p>
            </div>
            <%= link_to "상세 보기", parent_child_path(child), class: "bg-blue-600 text-white px-4 py-2 rounded text-sm" %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-gray-500">연결된 자녀가 없습니다. 학교 담당자에게 문의하세요.</p>
  <% end %>
</div>
