<div class="max-w-4xl mx-auto px-4 sm:px-6 py-8">
  <h1 class="page-title mb-8">자녀 학습 현황</h1>

  <% if @children.any? %>
    <div class="space-y-4">
      <% @children.each do |child| %>
        <div class="card p-5">
          <div class="flex justify-between items-center">
            <div>
              <h2 class="font-bold text-ink-900 text-lg mb-1.5"><%= child.name %></h2>
              <div class="flex flex-wrap gap-2">
                <span class="badge bg-primary-50 text-primary-700"><%= child.grade_level %>학년</span>
                <span class="badge bg-sage-50 text-sage-600">완료 학습: <%= child.learning_sessions.completed.count %>회</span>
              </div>
            </div>
            <%= link_to "상세 보기", parent_child_path(child), class: "btn-primary" %>
          </div>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="card-flat p-8 text-center">
      <p class="text-ink-500">연결된 자녀가 없습니다. 학교 담당자에게 문의하세요.</p>
    </div>
  <% end %>
</div>
