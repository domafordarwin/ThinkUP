<div class="max-w-3xl mx-auto p-6">
  <h1 class="text-2xl font-bold mb-2">자유 지문 학습</h1>
  <p class="text-gray-600 mb-6">학습하고 싶은 글을 직접 입력하세요. 입력한 지문으로 바로 학습을 시작합니다.</p>

  <%= form_with(model: @passage, url: free_passages_path, class: "space-y-4") do |f| %>
    <% if @passage.errors.any? %>
      <div class="bg-red-100 text-red-800 p-3 rounded">
        <% @passage.errors.full_messages.each do |msg| %>
          <p><%= msg %></p>
        <% end %>
      </div>
    <% end %>

    <div>
      <%= f.label :title, "제목", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_field :title, class: "w-full border rounded px-3 py-2", placeholder: "지문의 제목을 입력하세요", required: true %>
    </div>

    <div>
      <%= f.label :content, "지문 내용", class: "block text-sm font-medium text-gray-700 mb-1" %>
      <%= f.text_area :content, class: "w-full border rounded px-3 py-2 h-64", placeholder: "학습할 글을 붙여넣거나 직접 입력하세요...", required: true %>
    </div>

    <div class="flex items-center gap-4">
      <%= f.submit "학습 시작", class: "bg-blue-600 text-white px-6 py-2 rounded cursor-pointer" %>
      <%= link_to "취소", root_path, class: "text-gray-600" %>
    </div>
  <% end %>
</div>
