<div class="max-w-2xl mx-auto">
  <h1 class="page-title mb-2">자유 지문 학습</h1>
  <p class="text-gray-500 mb-8">학습하고 싶은 글을 직접 입력하세요. 입력한 지문으로 바로 학습을 시작합니다.</p>

  <div class="card-flat p-6 sm:p-8">
    <%= form_with(model: @passage, url: free_passages_path, class: "space-y-5") do |f| %>
      <% if @passage.errors.any? %>
        <div class="card p-4 border-l-4 border-red-400 text-red-600 text-sm">
          <% @passage.errors.full_messages.each do |msg| %>
            <p><%= msg %></p>
          <% end %>
        </div>
      <% end %>

      <div>
        <%= f.label :title, "제목", class: "block text-sm font-medium text-gray-700 mb-1.5" %>
        <%= f.text_field :title, class: "input-field", placeholder: "지문의 제목을 입력하세요", required: true %>
      </div>

      <div>
        <%= f.label :content, "지문 내용", class: "block text-sm font-medium text-gray-700 mb-1.5" %>
        <%= f.text_area :content, class: "input-field h-64", placeholder: "학습할 글을 붙여넣거나 직접 입력하세요...", required: true %>
        <p class="text-xs text-gray-400 mt-1.5">신문 기사, 수필, 소설 발췌문 등 어떤 글이든 자유롭게 입력할 수 있습니다.</p>
      </div>

      <div class="flex items-center gap-4 pt-2">
        <%= f.submit "학습 시작", class: "btn-primary btn-lg" %>
        <%= link_to "취소", root_path, class: "text-sm text-gray-400 hover:text-gray-600 transition-colors" %>
      </div>
    <% end %>
  </div>
</div>
