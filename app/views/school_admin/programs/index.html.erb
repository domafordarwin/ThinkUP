<div class="max-w-4xl mx-auto p-6">
  <h1 class="text-2xl font-bold mb-6">배정된 프로그램</h1>

  <% if @programs.any? %>
    <div class="space-y-4">
      <% @programs.each do |program| %>
        <div class="border rounded-lg p-4">
          <div class="flex justify-between items-center">
            <h2 class="font-semibold text-lg"><%= program.name %></h2>
            <span class="text-sm text-gray-500">
              <%= program.starts_on.strftime("%Y-%m-%d") %> ~ <%= program.ends_on.strftime("%Y-%m-%d") %>
            </span>
          </div>
          <p class="text-sm text-gray-600 mt-1">대상: <%= program.target_grade_min %>~<%= program.target_grade_max %>학년 | 지문 <%= program.passages.count %>개</p>
          <% if program.description.present? %>
            <p class="text-sm text-gray-500 mt-1"><%= program.description %></p>
          <% end %>
          <%= link_to "상세 보기", school_admin_program_path(program), class: "text-blue-600 text-sm mt-2 inline-block" %>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-gray-500">배정된 프로그램이 없습니다.</p>
  <% end %>
</div>
