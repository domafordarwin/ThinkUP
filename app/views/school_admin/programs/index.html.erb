<div class="max-w-4xl mx-auto px-4 sm:px-6 py-8">
  <h1 class="page-title mb-8">배정된 프로그램</h1>

  <% if @programs.any? %>
    <div class="space-y-4">
      <% @programs.each do |program| %>
        <div class="card p-5">
          <div class="flex justify-between items-start mb-3">
            <h2 class="font-semibold text-gray-900 text-lg"><%= program.name %></h2>
            <span class="badge bg-primary-50 text-primary-700">
              <%= program.starts_on.strftime("%Y-%m-%d") %> ~ <%= program.ends_on.strftime("%Y-%m-%d") %>
            </span>
          </div>
          <div class="flex flex-wrap gap-2 mb-3">
            <span class="badge bg-accent-50 text-accent-600"><%= program.target_grade_min %>~<%= program.target_grade_max %>학년</span>
            <span class="badge bg-warm-50 text-warm-600">지문 <%= program.passages.count %>개</span>
          </div>
          <% if program.description.present? %>
            <p class="text-sm text-gray-500 mb-3 leading-relaxed"><%= program.description %></p>
          <% end %>
          <%= link_to "상세 보기", school_admin_program_path(program), class: "btn-secondary text-sm" %>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="card-flat p-8 text-center">
      <p class="text-gray-500">배정된 프로그램이 없습니다.</p>
    </div>
  <% end %>
</div>
