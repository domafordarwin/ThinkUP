<div class="max-w-2xl mx-auto p-6 text-center">
  <h1 class="text-4xl font-bold mb-4">ThinkUp</h1>
  <p class="text-lg text-gray-600 mb-8">발문으로 키우는 사고력</p>

  <% if user_signed_in? && current_user.student? %>
    <% if @current_session %>
      <%= link_to "이어서 학습하기", learning_session_path(@current_session),
          class: "bg-blue-600 text-white px-8 py-3 rounded-lg text-lg" %>
    <% else %>
      <%= button_to "새 학습 시작", learning_sessions_path,
          method: :post, class: "bg-blue-600 text-white px-8 py-3 rounded-lg text-lg" %>
    <% end %>
  <% elsif user_signed_in? %>
    <p class="text-gray-500">역할: <%= current_user.role %></p>
  <% else %>
    <%= link_to "로그인", new_user_session_path, class: "bg-blue-600 text-white px-8 py-3 rounded-lg text-lg" %>
  <% end %>
</div>
